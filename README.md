## ranger-gitignore-filter

- a plugin for ranger file manager that can filter git-ignored files when show_hidden is not set
- install
  - git clone this repo into your `.config/ranger/plugins` or similar directory
